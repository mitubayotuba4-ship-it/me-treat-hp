/* ============================================
   Me Treat - AIチャットボット バックエンドサーバー
   Claude API（Anthropic）を使用したSSEストリーミング対応
   ============================================ */

require('dotenv').config();
const express = require('express');
const cors = require('cors');
const Anthropic = require('@anthropic-ai/sdk');

const app = express();
const PORT = process.env.PORT || 3000;

/* ---------- Anthropic クライアント初期化 ---------- */
const anthropic = new Anthropic({
  apiKey: process.env.ANTHROPIC_API_KEY,
});

/* ---------- ミドルウェア設定 ---------- */
app.use(cors({
  origin: ['http://localhost:5500', 'http://127.0.0.1:5500', 'http://localhost:3000', 'null'],
  methods: ['GET', 'POST'],
  credentials: true,
}));
app.use(express.json());

/* ---------- 静的ファイル配信（index.html等） ---------- */
app.use(express.static(__dirname));

/* ---------- Me Treat AIアシスタントのシステムプロンプト ---------- */
const SYSTEM_PROMPT = `あなたは「Me Treat」の公式AIアシスタントです。
さとう式リンパケアインストラクター Elymph（エリンフ）が運営するウェブサイトのチャットボットとして、訪問者のご質問に丁寧にお答えします。
以下の自社情報をもとに、正確にご案内ください。

【サービス名】
Me Treat

【サービス内容】
Me Treat は、さとう式リンパケアをベースに、以下の3つを提供しています。
1. 自分で体をケアできるようになる「セルフケア講座」
2. さとう式リンパケアに使えるイヤーカフの「作成講座（ビューティーカフ基礎講座）」
3. イヤーカフそのものの「販売」
身体をやさしく整えながら、「自分を大切にする時間」をサポートするサービスです。

また、さとう式リンパケアの資格「セルフケアマスター（SC）」取得後に開講できる講座についてのご案内も行っています。
セルフケアマスターを取得すると、以下の講座を自分で開講できるようになります。
- セルフケアマスター養成講座
- 頭皮セルフケア
- 各種セルフケア講座

【さとう式リンパケアについて】
- 「頑張ったら破門」という有名な教えがあります。力を入れれば入れるほど体は緊張するため、ゆるめることがケアの本質です。
- 「生まれ変わりのケア」とも呼ばれ、体の深部からゆるんでいきます。
- 押す・揉む・強くするのではなく、触れるか触れないかの優しさで体が変わります。
- 体と心の両方をゆるめることができる、全く新しいアプローチです。

【料金・講座詳細】

▶ セルフケア講座（セルフケアマスター養成講座）
　・受講料：165,000円（税込）〜
　・講座時間：6時間 × 3日間（合計18時間）
　・日程はご希望に合わせて調整可能
　・受講資格：どなたでも受講可能
　・修了後はご自身でも講座を開講できる資格が得られる
　・オンライン（Zoom）受講OK、全国対応

▶ ビューティーカフ基礎講座
　・受講料：15,000円（税込）〜
　・再受講料：7,500円（税込）〜（再受講制度あり・半額）
　・講座時間：2.5時間／1日
　・受講資格：どなたでも受講可能
　・世界にひとつの自分だけのイヤーカフを手作りできる
　・オンライン（Zoom）受講OK

▶ イヤーカフ販売
さとう式リンパケアのために生まれた特別なイヤーカフです。ケアしながらおしゃれも楽しめます。

・シンプルイヤーカフ：6,600円（税込）
　→ 飾りのないシンプルなデザイン。日常使いにも、ケアにも。

・テラヘルツイヤーカフ：7,700円（税込）
　→ テラヘルツ素材使用。より深いケア効果を求める方向け。

・チャーム（別売り）：価格はショップにてご確認ください
　→ カフに付けて、自分だけのスタイルに。種類もいろいろ。

購入はオンラインショップ（BASE）にてお受けしています。

【営業日・対応時間】
営業日：平日夜・土曜夜・日曜日
（土曜日は本業の勤務状況により、対応できない場合があります）
具体的な日時はご希望をお伺いして調整させていただきます。

【プロフィール（Elymph）】
- 元手術室看護師（20年）、看護師歴30年
- さとう式リンパケアインストラクター
- 終末期ケア専門士
- 看取り士
- ケアビューティスト
- 愛知県在住、オンライン全国対応

【お申し込み・お問い合わせ方法】
お申し込みやお問い合わせは、公式LINEから受け付けています。
ご希望のメニュー（セルフケア講座／ビューティーカフ基礎講座／イヤーカフ購入）と、
ご希望日時の候補をお知らせください。
※ 公式LINEのURLは準備中です。ご不便をおかけします。

【最新情報・発信】
最新情報やElymphの発信は以下でもご覧いただけます。
- Instagram：@elymph8888（https://www.instagram.com/elymph8888/）
- note：Elymph / Me Treat（https://note.com/mitreat）

【よくある質問と回答】

Q: 初めてでも受講できますか？
A: はい。セルフケア講座・ビューティーカフ基礎講座ともに、どなたでも受講可能です。さとう式リンパケアが初めての方にも、やさしくお伝えします。

Q: 講座の日程は固定ですか？調整できますか？
A: 基本の構成は「6時間 × 3日間（合計18時間）」ですが、日程はご希望に合わせて調整可能です。詳細はお問い合わせください。

Q: イヤーカフは普段使いもできますか？
A: はい。シンプルイヤーカフは日常使いにも向いたデザインで、ケア目的だけでなく、アクセサリーとしてもお楽しみいただけます。テラヘルツイヤーカフは、より深いケア効果を求める方向けです。

Q: 申し込み方法を教えてください。
A: お申し込みやお問い合わせは、公式LINEから受け付けています。ご希望のメニューと希望日時の候補をお知らせください。

【回答ルール：営業日・日程について】
ユーザーから「営業日」「いつやっていますか」「開催日程」「日時」などを聞かれたときは、必ず以下の3点をすべて伝えてください。

1. 営業日をはっきり伝える：
「営業日は平日夜・土曜夜・日曜日です。」
（土曜日は本業の勤務状況により対応できない場合があることも添えてください）

2. 日程は個別調整であることを伝える：
「具体的な日時は、ご希望をお伺いして個別に調整させていただきます。」

3. 問い合わせ先を案内する：
「お問い合わせやお申し込みは公式LINEから受け付けています。
また、Instagram（@elymph8888）やnote（note.com/mitreat）でも情報を発信しています。」

言い回しは多少変えても構いませんが、「平日夜・土曜夜・日曜日」「日程は個別調整」「公式LINE＋Instagram・noteを補足案内」の3点は必ず含めてください。

【返答のスタイル】
- 温かく、寄り添うような丁寧な口調で話してください。
- 簡潔にわかりやすくお答えください（長すぎる返答は避けてください）。
- 料金・日程・お申し込みについては、上記の正確な情報をもとにお答えください。
- お申し込みや詳細のご相談は、公式LINEへ誘導してください。
- わからないことは正直に「詳細はお問い合わせください」とお伝えください。
- 絵文字は控えめに、1〜2個程度にとどめてください。`;

/* ---------- チャットAPIエンドポイント（SSEストリーミング） ---------- */
app.post('/api/chat', async (req, res) => {
  const { messages } = req.body;

  // リクエストのバリデーション
  if (!messages || !Array.isArray(messages) || messages.length === 0) {
    return res.status(400).json({ error: 'メッセージが正しく送信されていません。' });
  }

  // SSEヘッダー設定
  res.setHeader('Content-Type', 'text/event-stream');
  res.setHeader('Cache-Control', 'no-cache');
  res.setHeader('Connection', 'keep-alive');
  res.setHeader('X-Accel-Buffering', 'no');
  res.flushHeaders();

  try {
    // Claude API ストリーミングリクエスト
    const stream = anthropic.messages.stream({
      model: 'claude-opus-4-6',
      max_tokens: 1024,
      thinking: { type: 'adaptive' },
      system: SYSTEM_PROMPT,
      messages: messages,
    });

    // テキストチャンクをSSEで送信
    stream.on('text', (text) => {
      res.write(`data: ${JSON.stringify({ type: 'text', text })}\n\n`);
    });

    // ストリーム完了
    await stream.finalMessage();
    res.write(`data: ${JSON.stringify({ type: 'done' })}\n\n`);
    res.end();

  } catch (error) {
    console.error('Claude APIエラー:', error);

    // エラーメッセージをクライアントに送信
    const errorMessage = error.status === 401
      ? 'APIキーが無効です。.envファイルを確認してください。'
      : 'AIとの接続に問題が発生しました。しばらくしてからお試しください。';

    res.write(`data: ${JSON.stringify({ type: 'error', message: errorMessage })}\n\n`);
    res.end();
  }
});

/* ---------- ヘルスチェックエンドポイント ---------- */
app.get('/api/health', (req, res) => {
  res.json({ status: 'ok', message: 'Me Treat チャットボットサーバー稼働中' });
});

/* ---------- サーバー起動 ---------- */
app.listen(PORT, () => {
  console.log(`\n✨ Me Treat チャットボットサーバー起動`);
  console.log(`   URL: http://localhost:${PORT}`);
  console.log(`   APIキー: ${process.env.ANTHROPIC_API_KEY ? '設定済み ✓' : '未設定 ✗ (.envファイルを確認してください)'}\n`);
});
